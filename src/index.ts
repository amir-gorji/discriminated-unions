export {
  match,
  matchWithDefault,
  map,
  mapAll,
  fold,
  count,
  partition,
  createPipeHandlers,
  createUnion,
  is,
  isUnion,
} from './unions';
export type { TakeDiscriminant, InferUnion, Folder } from './types';
