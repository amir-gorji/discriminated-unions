export {
  match,
  matchWithDefault,
  map,
  mapAll,
  narrow,
  fold,
  createPipeHandlers,
  createUnion,
  is,
  isUnion,
} from './unions';
export type { TakeDiscriminant, InferUnion, Folder } from './types';
