export {
  match,
  matchWithDefault,
  map,
  mapAll,
  fold,
  foldWithDefault,
  count,
  partition,
  createPipeHandlers,
  createUnion,
  is,
  isUnion,
} from './unions';
export type {
  TakeDiscriminant,
  InferUnion,
  Folder,
  FolderWithDefault,
} from './types';
